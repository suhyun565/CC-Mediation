python-dotenv>=1.0.0
openai>=1.30.0
requests>=2.31.0

# Optional — only needed for the local (non-OpenRouter) logprob DMIS labeler
# (metrics/llm_stage_judge_local.py). Skip if you only use the API path.
torch>=2.1.0
transformers>=4.40.0
